<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Web Terminal with xterm.js</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/xterm/css/xterm.css" />
  <style>
    body { margin: 0; background: #1e1e1e; color: #fff; }
    #terminal { height: 100vh; width: 100vw; }
  </style>
</head>
<body>
  <div id="terminal"></div>

  <script src="https://cdn.jsdelivr.net/npm/xterm/lib/xterm.js"></script>
  <script>
    const term = new Terminal();
    term.open(document.getElementById('terminal'));

    const userID = "student123";
    const ws = new WebSocket("ws://83.220.170.151:8765/terminal");

    let terminalReady = false;

    ws.onopen = () => {
      ws.send(JSON.stringify({ user_id: userID }));
    };

    ws.onmessage = (event) => {
      const data = event.data;

      // Если это JSON — проверим статус
      try {
        const parsed = JSON.parse(data);
        if (parsed.status === "ready") {
          terminalReady = true;
          term.focus();
          return;
        }
      } catch (_) {}

      // Обработка вывода: убираем мусор
      const clean = stripAnsiArtifacts(data);
      term.write(clean);
    };

    term.onData((data) => {
      if (terminalReady) {
        ws.send(data);
      }
    });

    function stripAnsiArtifacts(text) {
      // Удаляем OSC последовательности (например: ESC ]0;... BEL)
      text = text.replace(/\x1b\]0;.*?\x07/g, '');

      // Удаляем режим bracketed paste (ESC[?2004h / ESC[?2004l)
      text = text.replace(/\x1b\[\?2004[hl]/g, '');

      return text;
    }

    ws.onclose = () => {
      term.write('\r\n🔒 Соединение закрыто\r\n');
    };

    ws.onerror = (err) => {
      term.write(`\r\n❌ WebSocket error: ${err.message || err}\r\n`);
    };
  </script>
</body>
</html>
